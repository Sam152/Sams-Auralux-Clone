// Generated by CoffeeScript 1.6.3
(function() {
  window.UniformGrid = (function() {
    function UniformGrid(size) {
      this.size = size;
      this.grid_items = {};
    }

    UniformGrid.prototype.add = function(circle, item) {
      var key;
      key = this.getKey(circle.getX(), circle.getY());
      return this.addToCell(key, item);
    };

    UniformGrid.prototype.addToCell = function(cell, item) {
      this.grid_items[cell] = this.grid_items[cell] || [];
      return this.grid_items[cell].push(item);
    };

    UniformGrid.prototype.getCell = function(cell) {
      return this.grid_items[cell] || false;
    };

    UniformGrid.prototype.getKey = function(x, y) {
      return Math.floor(x / this.size) + 'x' + Math.floor(y / this.size);
    };

    UniformGrid.prototype.reset = function() {
      return this.grid_items = {};
    };

    UniformGrid.prototype.getItems = function() {
      return this.grid_items;
    };

    return UniformGrid;

  })();

}).call(this);
