// Generated by CoffeeScript 1.6.3
/*
A container for some DOM elements and the 2d context.
*/


(function() {
  window.Context = (function() {
    function Context(canvas) {
      var self;
      this.canvas = canvas;
      this.context2d = this.canvas.getContext('2d');
      this.$document = $(document);
      this.$body = $('body');
      this.setLineWidth(1);
      this.matchClientWidth();
      self = this;
      $(window).bind('resize', function() {
        return self.matchClientWidth.call(self);
      });
    }

    Context.prototype.matchClientWidth = function() {
      this.canvas.width = this.canvas.clientWidth;
      return this.canvas.height = this.canvas.clientHeight;
    };

    Context.prototype.get2d = function() {
      return this.context2d;
    };

    Context.prototype.getCanvas = function() {
      return this.canvas;
    };

    Context.prototype.getHeight = function() {
      return this.canvas.height;
    };

    Context.prototype.getWidth = function() {
      return this.canvas.width;
    };

    Context.prototype.setColor = function(color) {
      this.get2d().fillStyle = color;
      return this.get2d().strokeStyle = color;
    };

    Context.prototype.setLineWidth = function(width) {
      return this.get2d().lineWidth = width;
    };

    Context.prototype.clearScreen = function() {
      return this.get2d().clearRect(0, 0, this.canvas.width, this.canvas.height);
    };

    return Context;

  })();

}).call(this);
